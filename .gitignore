target
Cargo.lock

dist/
node_modules/
# Worktree post-checkout hooks link node_modules as a SYMLINK; the
# trailing-slash form above only matches real directories, so this
# extensionless form keeps the linked dirs out of `git add .` too.
node_modules

# Editor / OS cruft
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# celestia-devtools: staged-on-demand shared justfile recipes (just fetch)
/.just/
/celestia-devtools.just
